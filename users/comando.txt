#correr proyecto
python3 manage.py runserver
#correr proyecto con ipv4
python3 manage.py runserver 192.168.0.87:3000


#instalar bcrypt para encriptar constraseñas
pip3 install bcrypt

#obtener la ipv4 de mi mac 
ipconfig getifaddr en0

#CREAR TOKEN JWT
pip3 install djangorestframework-simplejwt


# crear una nueva app
python3 manage.py startapp authentication

# migrar un modelo a bd
python3 manage.py migrate
python3 manage.py migrate



injectar 
flutter packages pub run build_runner build

# 1. Cambia a la rama main
git checkout main

# 2. Asegúrate de tener la versión más reciente de main
git pull origin main

# 3. Crea y cambia a la nueva rama
git checkout -b feature/google-maps

# 4. (Opcional) Sube la nueva rama al repositorio remoto
git push -u origin feature/google-maps



